/**
 * MovieCard.styles.js - REMOVED
 * CODE_BIBLE Commandment #3: Write obvious code
 * 
 * All styles moved to MovieCard.js to match Home screen implementation exactly.
 * Home screen uses inline styles and homeStyles, not separate style files.
 */

// This file intentionally empty - styles are in MovieCard.js
export const getMovieCardStyles = () => ({});